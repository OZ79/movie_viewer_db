import 'package:flutter/material.dart';

class Genres extends StatelessWidget {
  const Genres();

  @override
  Widget build(BuildContext context) {
    return Container(
        height: 20, child: VerticalDivider(color: Colors.blue[600]));
  }
}
